# Vérifier si une clé publique existe déjà dans le répertoire utilisateur
ls -al ~/.ssh
# si contient id_rsa.pub id_ecdsa.pub id_ed25519.pub: copier la clé publique dans les réglages utilisateur de Github: Profil->Settings->SSH...
#sinon, consulter https://docs.github.com/fr/authentication/connecting-to-github-with-ssh/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent

git clone git@github.com:cledou/Cahier-de-labo.git